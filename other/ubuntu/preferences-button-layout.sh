#!/bin/bash

# Bring preference buttons to the left
gsettings set org.gnome.desktop.wm.preferences button-layout 'close,maximize,minimize:'

# Bring preference buttons to the right
# gsettings set org.gnome.desktop.wm.preferences button-layout ':close,maximize,minimize'
